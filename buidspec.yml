version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies"
      - npm install
      - npm install -g @angular/cli

  build:
    commands:
      - echo "Building the Angular application"
      - ng build

artifacts:
  files:
    - '**/*'
  base-directory: dist/HelloWorld

